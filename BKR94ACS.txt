Ben-Or, Kelmer, Rabin 1994 "Asynchronous Secure Computations with
Optimal Resilience (Extended Abstract)"
PODC '94, pages 183-192

Scope of this extract: Section 4 ("Agreement on a Common Subset"),
plus the Section 2 facts (communication and adversary model), the
Section 5 usage facts that bear on ACS callers, and the Section 1-3
facts on the asynchronous model's reconstruction limits and the
paper's own secret sharing. The linear combination, multiplication,
and Equality ZK Proof machinery, and the overall MPC construction,
remain unrelated to ACS as a standalone primitive and are
deliberately not summarized here.


Model and tools
---------------

n players (processors), fully connected asynchronous network, up to
t Byzantine faults. The MPC construction targets optimal resilience
 t < n/3 (i.e. n >= 3t + 1).

Channel and adversary model (paper Section 2): every two players are
connected by a secure and reliable channel — a message on it cannot
be heard by other players, is guaranteed to arrive, and may be
delayed arbitrarily (finitely). There is NO broadcast primitive in
the model; A-Cast is a simulation over the pairwise channels
(Bracha 1984). The adversary is computationally unbounded and
dynamic: it may corrupt up to t players adaptively at any time
(taking their private data and their control), and it schedules the
delay of every message, but it can neither read nor alter messages
between honest players. A deployment therefore owes exactly two
properties per PAIR — confidentiality ("secure") and eventual
delivery ("reliable") — and owes no broadcast medium.

Termination caveat, and where it does NOT attach: the paper notes its
MPC (and the Canetti-Rabin 1993 AVSS it builds on) carries an
exponentially small but non-zero probability of not terminating, and
proves (its Section 6) that this is unavoidable for AVSS when
n <= 4t. That caveat attaches to the AVSS (secret-sharing) side. The paper
itself contrasts "the asynchronous Byzantine Agreement problem where
the randomized protocol terminates with probability 1" — Bracha's,
the BA this repo instantiates the slot with.

ACS takes two subprotocols as inputs:

  A-Cast — asynchronous broadcast channel: reliable broadcast — if one
    honest player delivers v, every honest player eventually delivers v;
    all honest players deliver the same v; if the sender is honest,
    every honest player delivers. ACS is generic in this subprotocol;
    this repo uses Bracha 1987 Figure 1. (The primitive is the classic
    Bracha broadcast, originally Bracha 1984.)

  BA — binary asynchronous Byzantine Agreement: agreement, validity,
    and probabilistic termination. ACS is generic in this subprotocol —
    any binary BA with these properties works; the construction is not
    part of the ACS interface. This repo uses Bracha 1987 Figure 4
    (caller-supplied coin; probabilistic termination per Bracha
    Theorem 2/3, proof in Ben-Or 1983). BKR94's own MPC instantiated
    this slot with Canetti-Rabin 1993 — a paper-internal choice, not
    used here.


Problem setup
-------------

A dynamic predicate Q assigns a binary value to each player. "Dynamic"
means not all players' values are assigned at the same time; when
player Pj's value Q(j) is finally assigned, every honest player
eventually learns it. The protocol assumes two facts about Q:

  1. Every honest player eventually has Q = 1.
  2. Every honest player eventually learns the value of Q(j) for
     every j (whatever that value is).

Goal: the honest players agree on a subset of size at least n - t of
players for whom Q = 1.

The predicate Q is application-supplied. In BKR94's own MPC
construction, Q is "player P_j has properly shared his input" — Q(j)
becomes 1 for honest P_j after P_j's USS-Share completes, and honest
players learn this by observing the shared state. ACS is generic in Q;
any monotone "eventually-1-for-honest, eventually-known-to-all"
predicate works.


Protocol Agreement[Q]  (Figure 3)
---------------------------------

Code for player Pi. N BAs are run concurrently, one per player index
(BA_1 ... BA_n). Each player enters his inputs asynchronously as
evidence accumulates.

  1. For each Pj for whom you (Pi) know Q(j) = 1, participate in
     BA_j with input 1.

  2. Upon completing 2t+1 BA protocols with output 1, enter input 0
     to every BA protocol for which you have not yet entered a value.

  3. Upon completing all n BA protocols, let SubSet_i be the set of
     indices j for which BA_j had output 1. Output SubSet_i.

The "2t+1" threshold in step 2 is n - t in the paper's regime
(n = 3t+1). The paper cites [BOEY91] for a technique that runs the N
BAs with an O(log n) factor saving over naive independent execution;
it does not change the protocol's external behavior.


Lemma 2 (paper's statement, paraphrased)
----------------------------------------

Running Protocol Agreement[Q], the honest players agree on a common
subset SubSet of size at least 2t+1 of players Pj for whom Q(j) = 1.


Proof structure
---------------

The paper's proof has four obligations. Each uses only the three
named subprotocol properties (A-Cast agreement, BA agreement, BA
validity/termination) plus the two assumptions about Q.

[Part A] At least 2t+1 BA instances terminate with output 1.

  Case (i): Some honest player Pi entered a 0 value to some BA in
    step 2. The precondition of step 2 is "2t+1 BAs have already
    terminated with output 1" — done.

  Case (ii): No honest player ever entered 0. Then for each honest
    player Ph (assumption Q(h)=1 eventually), every honest player's
    input to BA_h is 1 (step 1). By BA's validity property (if all
    correct inputs are b, output is b), each such BA_h outputs 1.
    There are at least n - t = 2t+1 honest players, so at least 2t+1
    BAs output 1.

[Part B] All n BA instances terminate.

  By Part A, at least 2t+1 BAs output 1. By step 2 of the protocol,
  every honest player therefore enters an input (either 1 from step 1
  or 0 from step 2) to every BA. By BA's termination property, every
  BA terminates.

[Part C] All honest players agree on SubSet.

  After step 3 each honest player's SubSet has size >= 2t+1 (by Part
  A). BA's agreement property says every BA_j delivers the same
  output to every honest player, so every honest player has the same
  SubSet.

[Part D] For each j in SubSet, Q(j) = 1.

  BA_j terminated with output 1. By BA's validity property, at least
  one honest player entered 1 as input to BA_j. By step 1, that
  honest player does this only after learning Q(j) = 1. Since Q is
  "eventually known to all honest," Q(j) = 1 holds.


Remarks for implementers
------------------------

- The protocol is expressed in terms of what a single honest player
  does as events happen. Nothing is scheduled; all actions are
  triggered by completions (A-Cast deliveries of "Q(j)=1" evidence,
  BA terminations). The three steps describe "input to BA_j when you
  learn Q(j)=1", "input 0 to still-open BAs after 2t+1 1-outputs",
  and "read outputs once all n BAs finish."

- Step 1 and step 2 can fire concurrently for different j values.
  Once a BA has received an input from Pi (1 from step 1 or 0 from
  step 2), step 1 and step 2 stop touching it — BA semantics demand
  a single input per player.

- The 2t+1 threshold in step 2 is the n-t honest-majority bound. It
  cannot be lowered: without it a mass of 0-inputs could force BA_j
  to output 0 for all j (SubSet empty). It cannot be raised to
  require unanimity either: n-t is the maximum guarantee honest
  players have in an asynchronous network (they cannot wait for the
  t faulty players).

- Lemma 2 is silent about *which* players end up in SubSet beyond
  "Q(j) = 1". In particular, SubSet need not contain every honest
  player: an honest P_h whose Q-value propagates slowly, or whose
  "Q(h)=1" evidence reaches fewer than n-t honest players before
  step 2 fires for those players, may be excluded. Honest exclusion
  is a feature of the async model, not a bug.

- Per the paper, "To save a log n factor of running the n BAs, the
  techniques of [BOEY91] are employed." That is an optimization for
  the MPC use case where many Agreement[Q] calls happen; it does not
  change Lemma 2.


How the paper itself uses Agreement[Q]  (Section 5)
--------------------------------------------------------------------

- Invoked repeatedly, each invocation independent. The MPC calls it
  once at the input phase, once inside EVERY multiplication gate
  (Figure 5 step 7), and once per random-polynomial generation
  (Figure 8), each time with a fresh predicate Q. No state chains one
  invocation to the next: sequencing lives in the arithmetic circuit,
  and cross-invocation state travels in the shared secrets.

- Exclusion is by substitution. The function is evaluated with every
  excluded input substituted by 0, and the ideal-model definition
  (Section 2) even lets the ADVERSARY choose the subset — any CompSet
  of size at least n - t. Nothing ever re-enters an excluded input at
  a later invocation.

- Assumption 1 on Q ("every honest player eventually has Q = 1") is a
  per-invocation premise, and Lemma 2 Part A case (ii) consumes it
  directly: with every honest player's input to every BA equal to 1,
  each such BA outputs 1, so at least n - t = 2t + 1 BAs output 1.


The paper's answers to the async obstacle (Sections 1-3)
--------------------------------------------------------------------

Facts the paper states about its own construction in the t < n/3
asynchronous model. The obstacle (Section 1, "Obstacles to overcome";
Section 2): the adversary can delay honest players and force a
protocol to end with only n - t = 2t + 1 participants, the missing
ones not necessarily the faulty ones. Three answers the paper draws
from it:

- Late-player reconstruction (Sections 1, 3; Figure 2). The paper's
  Ultimate Secret Sharing meets the obstacle head on. Section 1,
  Overview: "there is nothing to prevent the adversary from delaying
  [the] honest players, and forcing the system to end the secret
  sharing protocol without their participation! We circumvent this by
  requiring that each share be distributed among all players. Thus, a
  late player can obtain his share by asking the system to reconstruct
  it." Reconstruction is USS-Rec (Figure 2): run the reconstruction
  for all shares, take any t + 1 values, interpolate. The USS
  Termination property (Section 3, Definition 1) guarantees that if
  one honest player has completed the sharing, every honest player
  eventually completes it, so the value a delayed player needs stays
  recoverable from the others.

- The honest-overlap count is t + 1, not 2t + 1 (Section 1; Section 2
  footnote). With n = 3t + 1, a value distributed to n - t players
  has, of those, only "n - 2t = t + 1 [that] are guaranteed to be
  honest, and that is exactly enough to enable the system to
  reconstruct the secret at a later stage" (Section 1). The Section 2
  footnote: "the asynchronous system must be able to compute when only
  n - t = 2t + 1 players are participating ... here we must proceed
  with only t + 1 honest players." A reconstruction threshold in this
  model is t + 1, never the synchronous 2t + 1.

- Detect, do not correct (Section 1). "As the models of both [BGW88,
  BCG93] ensure the participation of at least 3t + 1 players, they are
  able to use methods of error correcting codes to correct faulty
  shares. In our model, due to the asynchronicity of the network and
  the number t of faulty players, we can only count on the
  participation of 2t + 1 players. Thus, we cannot employ the methods
  of error correcting codes" (Section 1). The paper's secret sharing
  instead detects a faulty share -- its built-in self-correcting
  property (Section 3, Definition 1) -- and discards it, rather than
  correcting it.
